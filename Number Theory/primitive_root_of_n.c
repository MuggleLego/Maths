//date:2020.10.15
//input:an arbitrary integer n
//output:the smallet primitive root of n
//distinguish with primitive of prime p:
//we need to decide whether gcd(a,n)==1,if gcd(a,n) !=1,that a can't be a primitive root of n

